CREATE TABLE user_data (
     id INTEGER PRIMARY KEY AUTOINCREMENT,
     name TEXT NOT NULL
);

selectAll:
SELECT *
FROM user_data
ORDER BY name;

insertOrReplace:
INSERT OR REPLACE INTO user_data
VALUES (?, ?);

delete:
DELETE
FROM user_data
WHERE id = ?;

